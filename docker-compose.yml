services:
  wordpress:
    image: wordpress:fpm
    restart: unless-stopped
    environment:
      WORDPRESS_DB_HOST: ${WORDPRESS_DB_HOST:-mysql}
      WORDPRESS_DB_NAME: ${WORDPRESS_DB_NAME}
      WORDPRESS_DB_USER: ${WORDPRESS_DB_USER}
      WORDPRESS_DB_PASSWORD: ${WORDPRESS_DB_PASSWORD}
      WORDPRESS_TABLE_PREFIX: ${WORDPRESS_TABLE_PREFIX:-wp_}
      WORDPRESS_DEBUG: ${WORDPRESS_DEBUG:-false}
    volumes:
      - ${COMPOSE_PROJECT_NAME:-wordpress}_data:/var/www/html
    networks:
      - dokploy-network

  nginx:
    image: nginx:alpine
    restart: unless-stopped
    expose:
      - 80
    volumes:
      - ${COMPOSE_PROJECT_NAME:-wordpress}_data:/var/www/html:ro
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
    networks:
      - dokploy-network
    depends_on:
      - wordpress

volumes:
  ${COMPOSE_PROJECT_NAME:-wordpress}_data:
    driver: local

networks:
  dokploy-network:
    external: true
